# Redis Service — 클러스터 내부에서 "redis:6379"로 접근 가능
# app Pod가 Redis에 연결할 때 이 Service 이름을 사용
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: fitu
spec:
  type: ClusterIP
  selector:
    app: fitu-redis
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
